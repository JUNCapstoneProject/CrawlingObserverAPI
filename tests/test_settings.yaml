API_KEYS:
  Fred: "test-api-key"

symbol_size: 10

articles:
  size: 1
  retry: 1

save_method:
  save_to_file: true
  save_to_DB: true

database:
  url: "sqlite:///:memory:"

color_log: false
is_test: true
