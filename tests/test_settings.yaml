API_KEYS:
  Fred: "test-api-key"

symbol_size:
  total: 100

articles:
  size: 1
  retry: 1

save_method:
  save_to_file: true
  save_to_DB: true

database:
  url: "sqlite:///:memory:"

color_log: false
print_error_log: false

is_test:
  toggle: true
  interval: 1

crawler_switch:
  news: true
  reports: true
  financial: true
  stock: true

notifier_interval: 60

run_condition:
  crawler: true
  notifier: true
